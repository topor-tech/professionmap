name: Deploy Application

on:
  push:
    branches: [ trunk ]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build Docker Compose with Sentry
      run: |
        cd deploy
        docker compose build

    - name: Start services with Docker Compose
      run: |
        cd deploy
        docker compose up -d

    - name: Verify services are running
      run: |
        cd deploy
        docker compose ps