services:
  landing:
    build:
      context: ../src/front/landing
      dockerfile: Dockerfile
    ports:
      - "3333:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
